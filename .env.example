# Backend -> engine (TCP NDJSON)
ENGINE_HOST=127.0.0.1
ENGINE_PORT=8765
ENGINE_RECONNECT_SECONDS=2

# Path dibaca oleh proses backend
CAMERAS_CONFIG=backend/configs/cameras.yaml
POLICY_CONFIG=backend/configs/policy.yaml
