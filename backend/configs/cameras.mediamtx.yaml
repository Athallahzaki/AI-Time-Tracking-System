# MediaMTX mode: MediaMTX must expose the cam01 path before startup.
# HLS is selected for reliable PROGRAM-DATE-TIME overlay synchronization.
cameras:
  - id: "r1"
    code: "CAM-01"
    name: "Entertainment Room"
    source_uri: "rtsp://localhost:8554/cam01"
    stream_url: "http://localhost:8889/cam01/whep"
    enabled_by_default: true
    fps: 25

