# Local MP4 mode: no Docker or MediaMTX.
# Bounding boxes are synchronized with video.currentTime through engine PTS.
cameras:
  - id: "r1"
    code: "CAM-01"
    name: "Entertainment Room"
    source_uri: "frontend/public/videos/video2.mp4"
    stream_url: "/videos/video2.mp4"
    enabled_by_default: true
    fps: 25

